---
page_title: "netbox_vrf Data Source - NetBox"
subcategory: "IPAM"
description: |-
  Reads a single NetBox vrf (/api/ipam/vrfs/) by id, name or filters.
---

# netbox_vrf (Data Source)

Reads a single NetBox vrf (`/api/ipam/vrfs/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_vrf" "example" {
  name = "ACME-L3VPN"
}

# Any API filter of /api/ipam/vrfs/ works; the lookup must match exactly one object.
data "netbox_vrf" "filtered" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "vrf_id" {
  value = data.netbox_vrf.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/ipam/vrfs/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `enforce_unique` (Boolean) Prevent duplicate prefixes/IP addresses within this VRF. Defaults to the NetBox server default when omitted.
- `export_target_ids` (Set of Number) IDs of the assigned Export Target (`netbox_route_target`). Defaults to an empty set.
- `import_target_ids` (Set of Number) IDs of the assigned Import Target (`netbox_route_target`). Defaults to an empty set.
- `ipaddress_count` (Number) Ipaddress Count.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `prefix_count` (Number) Prefix Count.
- `rd` (String) Unique route distinguisher (as defined in RFC 4364).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
