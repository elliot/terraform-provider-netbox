---
page_title: "netbox_vlan_group Data Source - NetBox"
subcategory: "IPAM"
description: |-
  Reads a single NetBox vlan group (/api/ipam/vlan-groups/) by id, name, slug or filters.
---

# netbox_vlan_group (Data Source)

Reads a single NetBox vlan group (`/api/ipam/vlan-groups/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
data "netbox_vlan_group" "example" {
  slug = "dc1-vlans"
}

# Any API filter of /api/ipam/vlan-groups/ works; the lookup must match exactly one object.
data "netbox_vlan_group" "filtered" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "vlan_group_id" {
  value = data.netbox_vlan_group.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/ipam/vlan-groups/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `scope_id` (Number) Scope Id. Defaults to the NetBox server default when omitted.
- `scope_type` (String) Scope Type.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `total_vlan_ids` (Number) Total Vlan Ids.
- `url` (String) Url.
- `utilization` (String) Utilization.
- `vid_ranges` (List of List of Number) Vid Ranges.
- `vlan_count` (Number) Vlan Count.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
