---
page_title: "netbox_notification_groups Data Source - NetBox"
subcategory: "Extras"
description: |-
  Lists NetBox notification group objects (/api/extras/notification-groups/) matching the given filters.
---

# netbox_notification_groups (Data Source)

Lists NetBox notification group objects (`/api/extras/notification-groups/`) matching the given filters.

## Example Usage

```terraform
data "netbox_notification_groups" "all" {}

output "notification_group_names" {
  value = data.netbox_notification_groups.all.items[*].name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/extras/notification-groups/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name; validated against the parameters of the endpoint.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `group_ids` (Set of Number) IDs of the assigned Group (`netbox_user_group`). Defaults to an empty set.
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `url` (String) Url.
- `user_ids` (Set of Number) IDs of the assigned User (`netbox_user`). Defaults to an empty set.
