---
page_title: "netbox_ike_policy Data Source - NetBox"
subcategory: "VPN"
description: |-
  Reads a single NetBox ike policy (/api/vpn/ike-policies/) by id, name or filters.
---

# netbox_ike_policy (Data Source)

Reads a single NetBox ike policy (`/api/vpn/ike-policies/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_ike_policy" "by_name" {
  name = "IKEv2-PSK"
}

# Lookup by arbitrary API filters (any query parameter of the list endpoint):
data "netbox_ike_policy" "by_id" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "ike_policy_id" {
  value = data.netbox_ike_policy.by_name.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/vpn/ike-policies/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) All custom field values of the object as a JSON object (`jsondecode(...)`); selection values are unwrapped to the choice value and related objects to their ID.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `mode` (String) Mode. Valid values: `aggressive`, `main`. Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `preshared_key` (String, Sensitive) Preshared Key. Defaults to an empty string.
- `proposal_ids` (Set of Number) IDs of the assigned Proposal (`netbox_ike_proposal`). Defaults to an empty set.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
- `version` (Number) Version. Valid values: `1`, `2`.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
