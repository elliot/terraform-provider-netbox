---
page_title: "netbox_tunnel_group Data Source - NetBox"
subcategory: "VPN"
description: |-
  Reads a single NetBox tunnel group (/api/vpn/tunnel-groups/) by id, name, slug or filters.
---

# netbox_tunnel_group (Data Source)

Reads a single NetBox tunnel group (`/api/vpn/tunnel-groups/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
data "netbox_tunnel_group" "by_slug" {
  slug = "branch-vpn"
}

# Lookup by arbitrary API filters (any query parameter of the list endpoint):
data "netbox_tunnel_group" "by_id" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "tunnel_group_id" {
  value = data.netbox_tunnel_group.by_slug.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/vpn/tunnel-groups/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tunnel_count` (Number) Tunnel Count.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
