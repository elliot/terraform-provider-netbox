---
page_title: "netbox_provider_network Data Source - NetBox"
subcategory: "Circuits"
description: |-
  Reads a single NetBox provider network (/api/circuits/provider-networks/) by id, name or filters.
---

# netbox_provider_network (Data Source)

Reads a single NetBox provider network (`/api/circuits/provider-networks/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_provider_network" "mpls_eu" {
  name = "Lumen MPLS EU"
}

# Use it as the Z side of a circuit:
resource "netbox_circuit_termination" "z" {
  circuit_id       = netbox_circuit.example.id
  term_side        = "Z"
  termination_type = "circuits.providernetwork"
  termination_id   = data.netbox_provider_network.mpls_eu.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/circuits/provider-networks/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `provider_id` (Number) ID of the Provider (`netbox_provider`).
- `service_id` (String) Service Id. Defaults to an empty string.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
