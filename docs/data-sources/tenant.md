---
page_title: "netbox_tenant Data Source - NetBox"
subcategory: "Tenancy"
description: |-
  Reads a single NetBox tenant (/api/tenancy/tenants/) by id, name, slug or filters.
---

# netbox_tenant (Data Source)

Reads a single NetBox tenant (`/api/tenancy/tenants/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
data "netbox_tenant" "acme" {
  slug = "acme-corp"
}

resource "netbox_circuit" "example" {
  cid         = "LUM-DIA-48213"
  provider_id = netbox_provider.example.id
  type_id     = netbox_circuit_type.example.id
  tenant_id   = data.netbox_tenant.acme.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/tenancy/tenants/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `circuit_count` (Number) Circuit Count.
- `cluster_count` (Number) Cluster Count.
- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) All custom field values of the object as a JSON object (`jsondecode(...)`); selection values are unwrapped to the choice value and related objects to their ID.
- `description` (String) Description. Defaults to an empty string.
- `device_count` (Number) Device Count.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `group_id` (Number) ID of the Tenant Group (`netbox_tenant_group`).
- `ipaddress_count` (Number) Ipaddress Count.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `prefix_count` (Number) Prefix Count.
- `rack_count` (Number) Rack Count.
- `site_count` (Number) Site Count.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
- `virtualmachine_count` (Number) Virtualmachine Count.
- `vlan_count` (Number) Vlan Count.
- `vrf_count` (Number) Vrf Count.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name; validated against the parameters of the endpoint.
- `value` (String) Query parameter value.
