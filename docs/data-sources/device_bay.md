---
page_title: "netbox_device_bay Data Source - NetBox"
subcategory: "DCIM"
description: |-
  Reads a single NetBox device bay (/api/dcim/device-bays/) by id, name or filters.
---

# netbox_device_bay (Data Source)

Reads a single NetBox device bay (`/api/dcim/device-bays/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_device" "chassis" {
  name = "dc1-ucs-chassis01"
}
data "netbox_device_bay" "slot1" {
  filters = [
    { name = "device_id", value = data.netbox_device.chassis.id },
    { name = "name", value = "Slot 1" },
  ]
}
output "installed_blade" {
  value = data.netbox_device_bay.slot1.installed_device_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/dcim/device-bays/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `created` (String) Created.
- `custom_fields` (String) All custom field values of the object as a JSON object (`jsondecode(...)`); selection values are unwrapped to the choice value and related objects to their ID.
- `description` (String) Description. Defaults to an empty string.
- `device_id` (Number) ID of the Device (`netbox_device`).
- `display` (String) Display.
- `display_url` (String) Display Url.
- `enabled` (Boolean) Enabled. Defaults to the NetBox server default when omitted.
- `installed_device_id` (Number) ID of the Device (`netbox_device`).
- `label` (String) Physical label. Defaults to an empty string.
- `last_updated` (String) Last Updated.
- `occupied` (Boolean) Occupied.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
