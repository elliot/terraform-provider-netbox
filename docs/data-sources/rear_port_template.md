---
page_title: "netbox_rear_port_template Data Source - NetBox"
subcategory: "DCIM"
description: |-
  Reads a single NetBox rear port template (/api/dcim/rear-port-templates/) by id, name or filters.
---

# netbox_rear_port_template (Data Source)

Reads a single NetBox rear port template (`/api/dcim/rear-port-templates/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_device_type" "pp" {
  slug = "pp-24-lc"
}
data "netbox_rear_port_template" "example" {
  filters = [
    { name = "device_type_id", value = data.netbox_device_type.pp.id },
    { name = "name", value = "Trunk 1" },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/dcim/rear-port-templates/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) {module} is accepted as a substitution for the module bay position when attached to a module type.

### Read-Only

- `color` (String) Color. Defaults to an empty string.
- `created` (String) Created.
- `description` (String) Description. Defaults to an empty string.
- `device_type_id` (Number) ID of the Device Type (`netbox_device_type`).
- `display` (String) Display.
- `label` (String) Physical label. Defaults to an empty string.
- `last_updated` (String) Last Updated.
- `module_type_id` (Number) ID of the Module Type (`netbox_module_type`).
- `positions` (Number) Positions. Defaults to the NetBox server default when omitted.
- `type` (String) Type. One of 63 valid NetBox choices (see the NetBox documentation).
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
