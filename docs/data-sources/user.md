---
page_title: "netbox_user Data Source - NetBox"
subcategory: "Users"
description: |-
  Reads a single NetBox user (/api/users/users/) by id, username or filters.
---

# netbox_user (Data Source)

Reads a single NetBox user (`/api/users/users/`) by `id`, `username` or filters.

## Example Usage

```terraform
# Look up a single user by username.
data "netbox_user" "example" {
  username = "jdoe"
}

# Any API filter of /api/users/users/ works with filters; the lookup must match exactly one object.
data "netbox_user" "filtered" {
  filters = [
    { name = "group", value = "network-ops" },
  ]
}

output "user_id" {
  value = data.netbox_user.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/users/users/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `username` (String) Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.

### Read-Only

- `date_joined` (String) Date Joined. Defaults to the NetBox server default when omitted.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `email` (String) Email. Defaults to an empty string.
- `first_name` (String) First Name. Defaults to an empty string.
- `group_ids` (Set of Number) IDs of the assigned Group (`netbox_user_group`). Defaults to an empty set.
- `is_active` (Boolean) Designates whether this user should be treated as active. Unselect this instead of deleting accounts. Defaults to the NetBox server default when omitted.
- `last_login` (String) Last Login. Defaults to the NetBox server default when omitted.
- `last_name` (String) Last Name. Defaults to an empty string.
- `password` (String, Sensitive) Password. Write-only: NetBox does not return this value.
- `permission_ids` (Set of Number) IDs of the permissions assigned directly to this user (`netbox_permission`). Managed from `netbox_permission.user_ids`; read-only here.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
