---
page_title: "netbox_provider_accounts Data Source - NetBox"
subcategory: "Circuits"
description: |-
  Lists NetBox provider account objects (/api/circuits/provider-accounts/) matching the given filters.
---

# netbox_provider_accounts (Data Source)

Lists NetBox provider account objects (`/api/circuits/provider-accounts/`) matching the given filters.

## Example Usage

```terraform
data "netbox_provider_accounts" "lumen" {
  filters = [
    { name = "provider", value = "lumen" },
  ]
}

output "lumen_accounts" {
  value = data.netbox_provider_accounts.lumen.items[*].account
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/circuits/provider-accounts/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `account` (String) Account.
- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `name` (String) Name. Defaults to an empty string.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `provider_id` (Number) ID of the Provider (`netbox_provider`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
