---
page_title: "netbox_cluster_group Data Source - NetBox"
subcategory: "Virtualization"
description: |-
  Reads a single NetBox cluster group (/api/virtualization/cluster-groups/) by id, name, slug or filters.
---

# netbox_cluster_group (Data Source)

Reads a single NetBox cluster group (`/api/virtualization/cluster-groups/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
# Look up a single cluster group by slug.
data "netbox_cluster_group" "example" {
  slug = "emea"
}

# Any API filter of /api/virtualization/cluster-groups/ works with filters; the lookup must match exactly one object.
data "netbox_cluster_group" "filtered" {
  filters = [
    { name = "q", value = "emea" },
  ]
}

output "cluster_group_id" {
  value = data.netbox_cluster_group.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/virtualization/cluster-groups/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `cluster_count` (Number) Cluster Count.
- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
