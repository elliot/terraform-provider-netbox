---
page_title: "netbox_route_target Data Source - NetBox"
subcategory: "IPAM"
description: |-
  Reads a single NetBox route target (/api/ipam/route-targets/) by id, name or filters.
---

# netbox_route_target (Data Source)

Reads a single NetBox route target (`/api/ipam/route-targets/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_route_target" "example" {
  name = "65000:100"
}

# Any API filter of /api/ipam/route-targets/ works; the lookup must match exactly one object.
data "netbox_route_target" "filtered" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "route_target_id" {
  value = data.netbox_route_target.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/ipam/route-targets/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Route target value (formatted in accordance with RFC 4360).

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
