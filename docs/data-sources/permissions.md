---
page_title: "netbox_permissions Data Source - NetBox"
subcategory: "Users"
description: |-
  Lists NetBox permission objects (/api/users/permissions/) matching the given filters.
---

# netbox_permissions (Data Source)

Lists NetBox permission objects (`/api/users/permissions/`) matching the given filters.

## Example Usage

```terraform
# All permissions (paginated by NetBox; use limit to cap the result).
data "netbox_permissions" "all" {}

# Filters take any query parameter of /api/users/permissions/.
data "netbox_permissions" "filtered" {
  filters = [
    { name = "enabled", value = "true" },
  ]
  limit = 50
}

output "permissions_ids" {
  value = data.netbox_permissions.filtered.items[*].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/users/permissions/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `actions` (Set of String) The list of actions granted by this permission.
- `constraints` (String) Queryset filter matching the applicable objects of the selected type(s).
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `enabled` (Boolean) Enabled. Defaults to the NetBox server default when omitted.
- `group_ids` (Set of Number) IDs of the assigned Group (`netbox_user_group`). Defaults to an empty set.
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `object_types` (Set of String) Object Types.
- `url` (String) Url.
- `user_ids` (Set of Number) IDs of the assigned User (`netbox_user`). Defaults to an empty set.
