---
page_title: "netbox_virtual_machine_types Data Source - NetBox"
subcategory: "Virtualization"
description: |-
  Lists NetBox virtual machine type objects (/api/virtualization/virtual-machine-types/) matching the given filters.
---

# netbox_virtual_machine_types (Data Source)

Lists NetBox virtual machine type objects (`/api/virtualization/virtual-machine-types/`) matching the given filters.

## Example Usage

```terraform
# All virtual machine types (paginated by NetBox; use limit to cap the result).
data "netbox_virtual_machine_types" "all" {}

# Filters take any query parameter of /api/virtualization/virtual-machine-types/.
data "netbox_virtual_machine_types" "filtered" {
  filters = [
    { name = "q", value = "large" },
  ]
  limit = 50
}

output "virtual_machine_types_ids" {
  value = data.netbox_virtual_machine_types.filtered.items[*].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/virtualization/virtual-machine-types/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `default_memory` (Number) Default Memory. Defaults to the NetBox server default when omitted.
- `default_platform_id` (Number) ID of the Platform (`netbox_platform`).
- `default_vcpus` (Number) Default Vcpus. Defaults to the NetBox server default when omitted.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `name` (String) Name.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `slug` (String) Slug.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
- `virtual_machine_count` (Number) Virtual Machine Count.
