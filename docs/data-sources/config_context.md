---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netbox_config_context Data Source - NetBox"
subcategory: ""
description: |-
  Reads a single NetBox config context (/api/extras/config-contexts/) by id, name or filters.
---

# netbox_config_context (Data Source)

Reads a single NetBox config context (`/api/extras/config-contexts/`) by `id`, `name` or filters.

## Example Usage

```terraform
# Generated by internal/gen; edit generator/overrides to change, or remove this line to hand-maintain.
data "netbox_config_context" "example" {
  name = "example"
}

# Lookup by arbitrary API filters:
data "netbox_config_context" "filtered" {
  filters = [
    { name = "id", value = "123" },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/extras/config-contexts/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `cluster_group_ids` (Set of Number) IDs of the assigned Cluster Group (`netbox_cluster_group`). Defaults to the NetBox server default when omitted.
- `cluster_ids` (Set of Number) IDs of the assigned Cluster (`netbox_cluster`). Defaults to the NetBox server default when omitted.
- `cluster_type_ids` (Set of Number) IDs of the assigned Cluster Type (`netbox_cluster_type`). Defaults to the NetBox server default when omitted.
- `created` (String) Created.
- `data` (String) Data as a JSON document (`jsonencode({...})`).
- `data_file_id` (Number) Data File. References `netbox_data_file`.
- `data_path` (String) Path to remote file (relative to data source root).
- `data_source_id` (Number) ID of the Data Source (`netbox_data_source`).
- `data_synced` (String) Data Synced.
- `description` (String) Description. Defaults to the NetBox server default when omitted.
- `device_type_ids` (Set of Number) IDs of the assigned Device Type (`netbox_device_type`). Defaults to the NetBox server default when omitted.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `is_active` (Boolean) Is Active. Defaults to the NetBox server default when omitted.
- `last_updated` (String) Last Updated.
- `location_ids` (Set of Number) IDs of the assigned Location (`netbox_location`). Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `platform_ids` (Set of Number) IDs of the assigned Platform (`netbox_platform`). Defaults to the NetBox server default when omitted.
- `profile_id` (Number) ID of the Config Context Profile (`netbox_config_context_profile`).
- `region_ids` (Set of Number) IDs of the assigned Region (`netbox_region`). Defaults to the NetBox server default when omitted.
- `role_ids` (Set of Number) IDs of the assigned Role (`netbox_device_role`). Defaults to the NetBox server default when omitted.
- `site_group_ids` (Set of Number) IDs of the assigned Site Group (`netbox_site_group`). Defaults to the NetBox server default when omitted.
- `site_ids` (Set of Number) IDs of the assigned Site (`netbox_site`). Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Tags. Defaults to the NetBox server default when omitted.
- `tenant_group_ids` (Set of Number) IDs of the assigned Tenant Group (`netbox_tenant_group`). Defaults to the NetBox server default when omitted.
- `tenant_ids` (Set of Number) IDs of the assigned Tenant (`netbox_tenant`). Defaults to the NetBox server default when omitted.
- `url` (String) Url.
- `weight` (Number) Weight. Defaults to the NetBox server default when omitted.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
