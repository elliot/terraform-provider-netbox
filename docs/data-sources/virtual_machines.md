---
page_title: "netbox_virtual_machines Data Source - NetBox"
subcategory: "Virtualization"
description: |-
  Lists NetBox virtual machine objects (/api/virtualization/virtual-machines/) matching the given filters.
---

# netbox_virtual_machines (Data Source)

Lists NetBox virtual machine objects (`/api/virtualization/virtual-machines/`) matching the given filters.

## Example Usage

```terraform
# All virtual machines (paginated by NetBox; use limit to cap the result).
data "netbox_virtual_machines" "all" {}

# Filters take any query parameter of /api/virtualization/virtual-machines/.
data "netbox_virtual_machines" "filtered" {
  filters = [
    { name = "cluster", value = "fra1-prod-01" },
  ]
  limit = 50
}

output "virtual_machines_ids" {
  value = data.netbox_virtual_machines.filtered.items[*].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/virtualization/virtual-machines/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `cluster_id` (Number) ID of the Cluster (`netbox_cluster`).
- `comments` (String) Comments. Defaults to an empty string.
- `config_context` (String) Config Context.
- `config_template_id` (Number) ID of the Config Template (`netbox_config_template`).
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `device_id` (Number) ID of the Device (`netbox_device`).
- `disk` (Number) Total disk size in MB. Managed by NetBox once virtual disks are attached. Defaults to the NetBox server default when omitted.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `interface_count` (Number) Interface Count.
- `last_updated` (String) Last Updated.
- `local_context_data` (String) Local config context data takes precedence over source contexts in the final rendered config context.
- `memory` (Number) Memory. Defaults to the NetBox server default when omitted.
- `name` (String) Name.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `platform_id` (Number) ID of the Platform (`netbox_platform`).
- `primary_ip4_id` (Number) ID of the Ip Address (`netbox_ip_address`). Defaults to the NetBox server default when omitted.
- `primary_ip6_id` (Number) ID of the Ip Address (`netbox_ip_address`). Defaults to the NetBox server default when omitted.
- `primary_ip_id` (Number) Primary Ip. References `netbox_ip_address`.
- `role_id` (Number) ID of the Device Role (`netbox_device_role`).
- `serial` (String) Serial. Defaults to an empty string.
- `site_id` (Number) ID of the Site (`netbox_site`).
- `start_on_boot` (String) Start On Boot. Valid values: `on`, `off`, `laststate`. Defaults to the NetBox server default when omitted.
- `status` (String) Status. Valid values: `offline`, `active`, `planned`, `staged`, `failed`, `decommissioning`, `paused`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `url` (String) Url.
- `vcpus` (Number) Vcpus. Defaults to the NetBox server default when omitted.
- `virtual_disk_count` (Number) Virtual Disk Count.
- `virtual_machine_type_id` (Number) ID of the Virtual Machine Type (`netbox_virtual_machine_type`).
