---
page_title: "netbox_clusters Data Source - NetBox"
subcategory: "Virtualization"
description: |-
  Lists NetBox cluster objects (/api/virtualization/clusters/) matching the given filters.
---

# netbox_clusters (Data Source)

Lists NetBox cluster objects (`/api/virtualization/clusters/`) matching the given filters.

## Example Usage

```terraform
# All clusters (paginated by NetBox; use limit to cap the result).
data "netbox_clusters" "all" {}

# Filters take any query parameter of /api/virtualization/clusters/.
data "netbox_clusters" "filtered" {
  filters = [
    { name = "type", value = "vmware-vsphere" },
  ]
  limit = 50
}

output "clusters_ids" {
  value = data.netbox_clusters.filtered.items[*].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/virtualization/clusters/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `allocated_disk` (Number) Allocated Disk.
- `allocated_memory` (Number) Allocated Memory.
- `allocated_vcpus` (Number) Allocated Vcpus.
- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `device_count` (Number) Device Count.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `group_id` (Number) ID of the Cluster Group (`netbox_cluster_group`).
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `name` (String) Name.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `scope_id` (Number) Scope Id. Defaults to the NetBox server default when omitted.
- `scope_type` (String) Scope Type.
- `status` (String) Status. Valid values: `planned`, `staging`, `active`, `decommissioning`, `offline`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `type_id` (Number) ID of the Cluster Type (`netbox_cluster_type`).
- `url` (String) Url.
- `virtualmachine_count` (Number) Virtualmachine Count.
