---
page_title: "netbox_virtual_circuit_terminations Data Source - NetBox"
subcategory: "Circuits"
description: |-
  Lists NetBox virtual circuit termination objects (/api/circuits/virtual-circuit-terminations/) matching the given filters.
---

# netbox_virtual_circuit_terminations (Data Source)

Lists NetBox virtual circuit termination objects (`/api/circuits/virtual-circuit-terminations/`) matching the given filters.

## Example Usage

```terraform
data "netbox_virtual_circuit_terminations" "pw9001" {
  filters = [
    { name = "virtual_circuit_id", value = tostring(netbox_virtual_circuit.example.id) },
  ]
}

output "pw9001_interfaces" {
  value = data.netbox_virtual_circuit_terminations.pw9001.items[*].interface_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/circuits/virtual-circuit-terminations/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `interface_id` (Number) ID of the Interface (`netbox_interface`).
- `last_updated` (String) Last Updated.
- `role` (String) Role. Valid values: `peer`, `hub`, `spoke`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
- `virtual_circuit_id` (Number) ID of the Virtual Circuit (`netbox_virtual_circuit`).
