---
page_title: "netbox_vm_interface Data Source - NetBox"
subcategory: "Virtualization"
description: |-
  Reads a single NetBox vm interface (/api/virtualization/interfaces/) by id, name or filters.
---

# netbox_vm_interface (Data Source)

Reads a single NetBox vm interface (`/api/virtualization/interfaces/`) by `id`, `name` or filters.

## Example Usage

```terraform
# Look up a single vm interface by name.
data "netbox_vm_interface" "example" {
  name = "eth0"
}

# Any API filter of /api/virtualization/interfaces/ works with filters; the lookup must match exactly one object.
data "netbox_vm_interface" "filtered" {
  filters = [
    { name = "virtual_machine", value = "web01.example.com" },
  ]
}

output "vm_interface_id" {
  value = data.netbox_vm_interface.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/virtualization/interfaces/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `bridge_id` (Number) ID of the Vm Interface (`netbox_vm_interface`).
- `count_fhrp_groups` (Number) Count Fhrp Groups.
- `count_ipaddresses` (Number) Count Ipaddresses.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `enabled` (Boolean) Enabled. Defaults to the NetBox server default when omitted.
- `l2vpn_termination_id` (Number) L2vpn Termination. References `netbox_l2vpn_termination`.
- `last_updated` (String) Last Updated.
- `mac_address` (String) Mac Address.
- `mac_address_ids` (Set of Number) Mac Addresses. References `netbox_mac_address`.
- `mode` (String) IEEE 802.1Q tagging strategy. Valid values: `access`, `tagged`, `tagged-all`, `q-in-q`. Defaults to the NetBox server default when omitted.
- `mtu` (Number) Mtu. Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `parent_id` (Number) ID of the Vm Interface (`netbox_vm_interface`).
- `primary_mac_address_id` (Number) ID of the Mac Address (`netbox_mac_address`).
- `qinq_svlan_id` (Number) ID of the Vlan (`netbox_vlan`).
- `tagged_vlan_ids` (Set of Number) IDs of the assigned Tagged Vlan (`netbox_vlan`). Defaults to an empty set.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `untagged_vlan_id` (Number) ID of the Vlan (`netbox_vlan`).
- `url` (String) Url.
- `virtual_machine_id` (Number) ID of the Virtual Machine (`netbox_virtual_machine`).
- `vlan_translation_policy_id` (Number) ID of the Vlan Translation Policy (`netbox_vlan_translation_policy`).
- `vrf_id` (Number) ID of the Vrf (`netbox_vrf`).

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
