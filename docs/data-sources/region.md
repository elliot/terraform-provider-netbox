---
page_title: "netbox_region Data Source - NetBox"
subcategory: "DCIM"
description: |-
  Reads a single NetBox region (/api/dcim/regions/) by id, name, slug or filters.
---

# netbox_region (Data Source)

Reads a single NetBox region (`/api/dcim/regions/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
# Look up a region by its unique slug.
data "netbox_region" "example" {
  slug = "europe"
}

# Or by any API filter (see the NetBox filter documentation for /api/dcim/regions/).
data "netbox_region" "by_id" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "region_id" {
  value = data.netbox_region.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/dcim/regions/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `depth` (Number) Depth.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `parent_id` (Number) ID of the Region (`netbox_region`).
- `prefix_count` (Number) Prefix Count.
- `site_count` (Number) Site Count.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
