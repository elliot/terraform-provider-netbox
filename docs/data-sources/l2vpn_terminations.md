---
page_title: "netbox_l2vpn_terminations Data Source - NetBox"
subcategory: "VPN"
description: |-
  Lists NetBox l2vpn termination objects (/api/vpn/l2vpn-terminations/) matching the given filters.
---

# netbox_l2vpn_terminations (Data Source)

Lists NetBox l2vpn termination objects (`/api/vpn/l2vpn-terminations/`) matching the given filters.

## Example Usage

```terraform
# All terminations of one L2VPN.
data "netbox_l2vpn_terminations" "tenant_a" {
  filters = [
    { name = "l2vpn_id", value = tostring(netbox_l2vpn.tenant_a.id) },
  ]
}

output "tenant_a_termination_objects" {
  value = data.netbox_l2vpn_terminations.tenant_a.items[*].assigned_object_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/vpn/l2vpn-terminations/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `assigned_object_id` (Number) Assigned Object Id.
- `assigned_object_type` (String) Assigned Object Type.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `l2vpn_id` (Number) ID of the L2vpn (`netbox_l2vpn`).
- `last_updated` (String) Last Updated.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
