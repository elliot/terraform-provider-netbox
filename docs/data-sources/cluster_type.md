---
page_title: "netbox_cluster_type Data Source - NetBox"
subcategory: "Virtualization"
description: |-
  Reads a single NetBox cluster type (/api/virtualization/cluster-types/) by id, name, slug or filters.
---

# netbox_cluster_type (Data Source)

Reads a single NetBox cluster type (`/api/virtualization/cluster-types/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
# Look up a single cluster type by slug.
data "netbox_cluster_type" "example" {
  slug = "vmware-vsphere"
}

# Any API filter of /api/virtualization/cluster-types/ works with filters; the lookup must match exactly one object.
data "netbox_cluster_type" "filtered" {
  filters = [
    { name = "q", value = "vmware" },
  ]
}

output "cluster_type_id" {
  value = data.netbox_cluster_type.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/virtualization/cluster-types/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `cluster_count` (Number) Cluster Count.
- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) All custom field values of the object as a JSON object (`jsondecode(...)`); selection values are unwrapped to the choice value and related objects to their ID.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
