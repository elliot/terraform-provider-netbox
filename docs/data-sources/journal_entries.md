---
page_title: "netbox_journal_entries Data Source - NetBox"
subcategory: "Extras"
description: |-
  Lists NetBox journal entry objects (/api/extras/journal-entries/) matching the given filters.
---

# netbox_journal_entries (Data Source)

Lists NetBox journal entry objects (`/api/extras/journal-entries/`) matching the given filters.

## Example Usage

```terraform
# Journal entries of one site, newest first.
data "netbox_journal_entries" "site" {
  filters = [
    { name = "assigned_object_type", value = "dcim.site" },
    { name = "assigned_object_id", value = tostring(netbox_site.example.id) },
    { name = "ordering", value = "-created" },
  ]
  limit = 20
}

output "site_journal" {
  value = data.netbox_journal_entries.site.items[*].comments
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/extras/journal-entries/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `assigned_object_id` (Number) Assigned Object Id.
- `assigned_object_type` (String) Assigned Object Type.
- `comments` (String) Comments.
- `created` (String) Created.
- `created_by_id` (Number) ID of the User (`netbox_user`). Defaults to the NetBox server default when omitted.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `kind` (String) Kind. Valid values: `info`, `success`, `warning`, `danger`. Defaults to the NetBox server default when omitted.
- `last_updated` (String) Last Updated.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.
