---
page_title: "netbox_power_panel Data Source - NetBox"
subcategory: "DCIM"
description: |-
  Reads a single NetBox power panel (/api/dcim/power-panels/) by id, name or filters.
---

# netbox_power_panel (Data Source)

Reads a single NetBox power panel (`/api/dcim/power-panels/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_site" "fra1" {
  slug = "fra1"
}

# Power panel names are unique per site.
data "netbox_power_panel" "pp_a" {
  filters = [
    { name = "site_id", value = data.netbox_site.fra1.id },
    { name = "name", value = "PP-A" },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/dcim/power-panels/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) All custom field values of the object as a JSON object (`jsondecode(...)`); selection values are unwrapped to the choice value and related objects to their ID.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `last_updated` (String) Last Updated.
- `location_id` (Number) ID of the Location (`netbox_location`).
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `powerfeed_count` (Number) Powerfeed Count.
- `site_id` (Number) ID of the Site (`netbox_site`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
