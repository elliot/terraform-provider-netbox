---
page_title: "netbox_asn_range Data Source - NetBox"
subcategory: "IPAM"
description: |-
  Reads a single NetBox asn range (/api/ipam/asn-ranges/) by id, name, slug or filters.
---

# netbox_asn_range (Data Source)

Reads a single NetBox asn range (`/api/ipam/asn-ranges/`) by `id`, `name`, `slug` or filters.

## Example Usage

```terraform
data "netbox_asn_range" "example" {
  slug = "fabric-underlay"
}

# Any API filter of /api/ipam/asn-ranges/ works; the lookup must match exactly one object.
data "netbox_asn_range" "filtered" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "asn_range_id" {
  value = data.netbox_asn_range.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/ipam/asn-ranges/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `slug` (String) Slug.

### Read-Only

- `asn_count` (Number) Asn Count.
- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `end` (Number) End.
- `last_updated` (String) Last Updated.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `rir_id` (Number) ID of the Rir (`netbox_rir`).
- `start` (Number) Start.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.
