---
page_title: "netbox_vlan_translation_policies Data Source - NetBox"
subcategory: "IPAM"
description: |-
  Lists NetBox vlan translation policy objects (/api/ipam/vlan-translation-policies/) matching the given filters.
---

# netbox_vlan_translation_policies (Data Source)

Lists NetBox vlan translation policy objects (`/api/ipam/vlan-translation-policies/`) matching the given filters.

## Example Usage

```terraform
data "netbox_vlan_translation_policies" "all" {}

data "netbox_vlan_translation_policies" "filtered" {
  filters = [
    { name = "q", value = "customer" },
  ]
  limit = 50
}

output "vlan_translation_policies_ids" {
  value = data.netbox_vlan_translation_policies.filtered.items[*].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/ipam/vlan-translation-policies/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name; validated against the parameters of the endpoint.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `comments` (String) Comments. Defaults to an empty string.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `rule_ids` (Set of Number) Rules. References `netbox_vlan_translation_rule`.
- `url` (String) Url.
