---
page_title: "netbox_ipsec_profile Data Source - NetBox"
subcategory: "VPN"
description: |-
  Reads a single NetBox ipsec profile (/api/vpn/ipsec-profiles/) by id, name or filters.
---

# netbox_ipsec_profile (Data Source)

Reads a single NetBox ipsec profile (`/api/vpn/ipsec-profiles/`) by `id`, `name` or filters.

## Example Usage

```terraform
data "netbox_ipsec_profile" "by_name" {
  name = "branch-site-to-site"
}

# Lookup by arbitrary API filters (any query parameter of the list endpoint):
data "netbox_ipsec_profile" "by_id" {
  filters = [
    { name = "id", value = "123" },
  ]
}

output "ipsec_profile_id" {
  value = data.netbox_ipsec_profile.by_name.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/vpn/ipsec-profiles/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `id` (Number) The numeric ID of the object in NetBox.
- `name` (String) Name.

### Read-Only

- `comments` (String) Comments. Defaults to an empty string.
- `created` (String) Created.
- `custom_fields` (String) All custom field values of the object as a JSON object (`jsondecode(...)`); selection values are unwrapped to the choice value and related objects to their ID.
- `description` (String) Description. Defaults to an empty string.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `ike_policy_id` (Number) ID of the Ike Policy (`netbox_ike_policy`).
- `ipsec_policy_id` (Number) ID of the Ipsec Policy (`netbox_ipsec_policy`).
- `last_updated` (String) Last Updated.
- `mode` (String) Mode. Valid values: `esp`, `ah`.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `url` (String) Url.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name; validated against the parameters of the endpoint.
- `value` (String) Query parameter value.
