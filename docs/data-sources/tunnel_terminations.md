---
page_title: "netbox_tunnel_terminations Data Source - NetBox"
subcategory: "VPN"
description: |-
  Lists NetBox tunnel termination objects (/api/vpn/tunnel-terminations/) matching the given filters.
---

# netbox_tunnel_terminations (Data Source)

Lists NetBox tunnel termination objects (`/api/vpn/tunnel-terminations/`) matching the given filters.

## Example Usage

```terraform
# All terminations of one tunnel.
data "netbox_tunnel_terminations" "gre_branch01" {
  filters = [
    { name = "tunnel_id", value = tostring(netbox_tunnel.gre_branch01.id) },
  ]
}

output "gre_branch01_termination_roles" {
  value = data.netbox_tunnel_terminations.gre_branch01.items[*].role
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) Additional query filters as name/value pairs, e.g. `{ name = "tenant_id", value = "12" }`. Any query parameter of `/api/vpn/tunnel-terminations/` is accepted. (see [below for nested schema](#nestedatt--filters))
- `limit` (Number) Maximum number of objects to return (default: all).

### Read-Only

- `items` (Attributes List) The matching objects. (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `name` (String) Query parameter name.
- `value` (String) Query parameter value.


<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `created` (String) Created.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `display` (String) Display.
- `display_url` (String) Display Url.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `outside_ip_id` (Number) ID of the Ip Address (`netbox_ip_address`).
- `role` (String) Role. Valid values: `peer`, `hub`, `spoke`.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `termination_id` (Number) Termination Id. Defaults to the NetBox server default when omitted.
- `termination_type` (String) Termination Type.
- `tunnel_id` (Number) ID of the Tunnel (`netbox_tunnel`).
- `url` (String) Url.
