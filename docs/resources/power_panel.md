---
page_title: "netbox_power_panel Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox power panel (/api/dcim/power-panels/).
---

# netbox_power_panel (Resource)

Manages a NetBox power panel (`/api/dcim/power-panels/`).

## Example Usage

```terraform
resource "netbox_site" "fra1" {
  name = "FRA1"
  slug = "fra1"
}

resource "netbox_location" "electrical_room" {
  name    = "Electrical room"
  slug    = "fra1-electrical-room"
  site_id = netbox_site.fra1.id
}

resource "netbox_tag" "power" {
  name = "power"
  slug = "power"
}

resource "netbox_power_panel" "pdu_a" {
  site_id     = netbox_site.fra1.id
  location_id = netbox_location.electrical_room.id
  name        = "PP-A"
  description = "Distribution panel, A side"
  tags        = [netbox_tag.power.slug]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `site_id` (Number) ID of the Site (`netbox_site`).

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `location_id` (Number) ID of the Location (`netbox_location`).
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_power_panel is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_power_panel.example 123
```
