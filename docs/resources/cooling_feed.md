---
page_title: "netbox_cooling_feed Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox cooling feed (/api/dcim/cooling-feeds/).
---

# netbox_cooling_feed (Resource)

Manages a NetBox cooling feed (`/api/dcim/cooling-feeds/`).

## Example Usage

```terraform
resource "netbox_site" "fra1" {
  name = "FRA1"
  slug = "fra1"
}

resource "netbox_cooling_source" "chiller_1" {
  site_id = netbox_site.fra1.id
  name    = "CH-01"
  type    = "chiller"
}

resource "netbox_rack" "a01" {
  name    = "A01"
  site_id = netbox_site.fra1.id
}

resource "netbox_cooling_feed" "a01_loop" {
  cooling_source_id = netbox_cooling_source.chiller_1.id
  rack_id           = netbox_rack.a01.id
  name              = "CH-01/A01"
  status            = "active"
  cooling_capacity  = 30
  max_flow          = 12.5
  max_flow_unit     = "lpm"
  description       = "Coolant loop to rack A01"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cooling_source_id` (Number) ID of the Cooling Source (`netbox_cooling_source`).
- `name` (String) Name.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `cooling_capacity` (Number) Rated cooling capacity (kW). Defaults to the NetBox server default when omitted.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `max_flow` (Number) Max Flow. Defaults to the NetBox server default when omitted.
- `max_flow_unit` (String) Max Flow Unit. Valid values: `lpm`, `m3ph`, `gpm`. Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `rack_id` (Number) ID of the Rack (`netbox_rack`).
- `status` (String) Status. Valid values: `offline`, `active`, `planned`, `failed`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_cooling_feed is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_cooling_feed.example 123
```
