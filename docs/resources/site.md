---
page_title: "netbox_site Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox site (/api/dcim/sites/).
---

# netbox_site (Resource)

Manages a NetBox site (`/api/dcim/sites/`).

## Example Usage

```terraform
resource "netbox_region" "germany" {
  name = "Germany"
  slug = "de"
}

resource "netbox_site_group" "datacenters" {
  name = "Data centres"
  slug = "datacenters"
}

resource "netbox_tenant" "acme" {
  name = "ACME Corp"
  slug = "acme"
}

resource "netbox_tag" "production" {
  name = "production"
  slug = "production"
}

resource "netbox_site" "fra1" {
  name             = "FRA1"
  slug             = "fra1"
  status           = "active"
  region_id        = netbox_region.germany.id
  group_id         = netbox_site_group.datacenters.id
  tenant_id        = netbox_tenant.acme.id
  facility         = "Interxion FRA1"
  time_zone        = "Europe/Berlin"
  description      = "Primary data centre in Frankfurt"
  physical_address = "Hanauer Landstraße 300, 60314 Frankfurt am Main, Germany"
  shipping_address = "Hanauer Landstraße 300, 60314 Frankfurt am Main, Germany (loading dock B)"
  latitude         = 50.114500
  longitude        = 8.735500
  comments         = "Managed by Terraform"
  tags             = [netbox_tag.production.slug]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Full name of the site.
- `slug` (String) Slug.

### Optional

- `asn_ids` (Set of Number) IDs of the assigned Asn (`netbox_asn`). Defaults to an empty set.
- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (Dynamic) Custom field values as an object of field name to value, e.g. `{ cost_center = "CC-42", vlan_id = 5, owner_site = 12 }`. Selection fields take the choice value, object fields the related object ID, multi-value fields a list, JSON fields any value. Only keys present in the configuration are tracked; field names are validated against the NetBox definitions.
- `description` (String) Description. Defaults to an empty string.
- `facility` (String) Local facility ID or description. Defaults to an empty string.
- `group_id` (Number) ID of the Site Group (`netbox_site_group`).
- `latitude` (Number) GPS coordinate in decimal format (xx.yyyyyy). Defaults to the NetBox server default when omitted.
- `longitude` (Number) GPS coordinate in decimal format (xx.yyyyyy). Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `physical_address` (String) Physical location of the building. Defaults to an empty string.
- `region_id` (Number) ID of the Region (`netbox_region`).
- `shipping_address` (String) If different from the physical address. Defaults to an empty string.
- `status` (String) Status. Valid values: `planned`, `staging`, `active`, `decommissioning`, `retired`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `time_zone` (String) Time Zone.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_site is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_site.example 123
```
