---
page_title: "netbox_cooling_intake_template Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox cooling intake template (/api/dcim/cooling-intake-templates/).
---

# netbox_cooling_intake_template (Resource)

Manages a NetBox cooling intake template (`/api/dcim/cooling-intake-templates/`).

## Example Usage

```terraform
resource "netbox_manufacturer" "example" {
  name = "Example Networks"
  slug = "example-networks"
}

resource "netbox_device_type" "sw48" {
  manufacturer_id = netbox_manufacturer.example.id
  model           = "SW-48"
  slug            = "example-sw-48"
  u_height        = 1
}

resource "netbox_cooling_intake_template" "in1" {
  device_type_id = netbox_device_type.sw48.id
  name           = "Intake 1"
  label          = "Coolant in"
  type           = "uqd"
  diameter       = 12.7
  diameter_unit  = "mm"
  max_flow       = 4.5
  max_flow_unit  = "lpm"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) {module} is accepted as a substitution for the module bay position when attached to a module type.

### Optional

- `description` (String) Description. Defaults to an empty string.
- `device_type_id` (Number) ID of the Device Type (`netbox_device_type`).
- `diameter` (Number) Diameter. Defaults to the NetBox server default when omitted.
- `diameter_unit` (String) Diameter Unit. Valid values: `mm`, `cm`, `in`. Defaults to the NetBox server default when omitted.
- `label` (String) Physical label. Defaults to an empty string.
- `max_flow` (Number) Max Flow. Defaults to the NetBox server default when omitted.
- `max_flow_unit` (String) Max Flow Unit. Valid values: `lpm`, `m3ph`, `gpm`. Defaults to the NetBox server default when omitted.
- `module_type_id` (Number) ID of the Module Type (`netbox_module_type`).
- `type` (String) Type. Valid values: `uqd`, `uqdb`, `qdc`, `camlock`, `npt`, `bsp`, `proprietary`. Defaults to the NetBox server default when omitted.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_cooling_intake_template is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_cooling_intake_template.example 123
```
