---
page_title: "netbox_rir Resource - NetBox"
subcategory: "IPAM"
description: |-
  Manages a NetBox RIR (/api/ipam/rirs/).
---

# netbox_rir (Resource)

Manages a NetBox RIR (`/api/ipam/rirs/`).

## Example Usage

```terraform
resource "netbox_rir" "rfc1918" {
  name        = "RFC 1918"
  slug        = "rfc-1918"
  is_private  = true
  description = "Private IPv4 address space"
}

resource "netbox_rir" "ripe" {
  name = "RIPE NCC"
  slug = "ripe-ncc"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `slug` (String) Slug.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `is_private` (Boolean) IP space managed by this RIR is considered private. Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_rir is imported by its NetBox object ID (the number in the object's URL).
terraform import netbox_rir.example 123
```
