---
page_title: "netbox_power_feed Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox power feed (/api/dcim/power-feeds/).
---

# netbox_power_feed (Resource)

Manages a NetBox power feed (`/api/dcim/power-feeds/`).

## Example Usage

```terraform
resource "netbox_site" "fra1" {
  name = "FRA1"
  slug = "fra1"
}

resource "netbox_power_panel" "pdu_a" {
  site_id = netbox_site.fra1.id
  name    = "PP-A"
}

resource "netbox_rack" "a01" {
  name    = "A01"
  site_id = netbox_site.fra1.id
}

resource "netbox_tag" "power" {
  name = "power"
  slug = "power"
}

# Feeds from the panel to the rack. The A/B pair share a rack but come from
# different panels in a real deployment.
resource "netbox_power_feed" "a01_a" {
  power_panel_id  = netbox_power_panel.pdu_a.id
  rack_id         = netbox_rack.a01.id
  name            = "A01-A"
  status          = "active"
  type            = "primary"
  supply          = "ac"
  phase           = "single-phase"
  voltage         = 230
  amperage        = 32
  max_utilization = 80
  description     = "Primary feed for rack A01"
  tags            = [netbox_tag.power.slug]
}

resource "netbox_power_feed" "a01_b" {
  power_panel_id = netbox_power_panel.pdu_a.id
  rack_id        = netbox_rack.a01.id
  name           = "A01-B"
  type           = "redundant"
  supply         = "ac"
  phase          = "single-phase"
  voltage        = 230
  amperage       = 32
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `power_panel_id` (Number) ID of the Power Panel (`netbox_power_panel`).

### Optional

- `amperage` (Number) Amperage. Defaults to the NetBox server default when omitted.
- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `mark_connected` (Boolean) Treat as if a cable is connected. Defaults to the NetBox server default when omitted.
- `max_utilization` (Number) Maximum permissible draw (percentage). Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `phase` (String) Phase. Valid values: `single-phase`, `three-phase`. Defaults to the NetBox server default when omitted.
- `rack_id` (Number) ID of the Rack (`netbox_rack`).
- `status` (String) Status. Valid values: `offline`, `active`, `planned`, `failed`. Defaults to the NetBox server default when omitted.
- `supply` (String) Supply. Valid values: `ac`, `dc`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `type` (String) Type. Valid values: `primary`, `redundant`. Defaults to the NetBox server default when omitted.
- `voltage` (Number) Voltage. Defaults to the NetBox server default when omitted.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_power_feed is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_power_feed.example 123
```
