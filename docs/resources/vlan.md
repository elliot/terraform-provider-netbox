---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netbox_vlan Resource - NetBox"
subcategory: ""
description: |-
  Manages a NetBox VLAN (/api/ipam/vlans/).
---

# netbox_vlan (Resource)

Manages a NetBox VLAN (`/api/ipam/vlans/`).

## Example Usage

```terraform
# Generated by internal/gen; edit generator/overrides to change, or remove this line to hand-maintain.
resource "netbox_tag" "example" {
  name = "example-tag"
  slug = "example-tag"
}

resource "netbox_vlan" "example" {
  vid = 1
  name = "example"
  description = "Managed by Terraform"
  tags = [netbox_tag.example.slug]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `vid` (Number) Numeric VLAN ID (1-4094).

### Optional

- `comments` (String) Comments. Defaults to the NetBox server default when omitted.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to the NetBox server default when omitted.
- `group_id` (Number) ID of the Vlan Group (`netbox_vlan_group`).
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `qinq_role` (String) Customer/service VLAN designation (for Q-in-Q/IEEE 802.1ad). Valid values: `svlan`, `cvlan`. Defaults to the NetBox server default when omitted.
- `qinq_svlan_id` (Number) ID of the Vlan (`netbox_vlan`).
- `role_id` (Number) ID of the Ipam Role (`netbox_ipam_role`).
- `site_id` (Number) ID of the Site (`netbox_site`).
- `status` (String) Operational status of this VLAN. Valid values: `active`, `reserved`, `deprecated`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to the NetBox server default when omitted.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Generated by internal/gen; edit generator/overrides to change, or remove this line to hand-maintain.
# netbox_vlan can be imported by its NetBox object ID.
terraform import netbox_vlan.example 123
```
