---
page_title: "netbox_vlan_translation_rule Resource - NetBox"
subcategory: "IPAM"
description: |-
  Manages a NetBox VLAN translation rule (/api/ipam/vlan-translation-rules/).
---

# netbox_vlan_translation_rule (Resource)

Manages a NetBox VLAN translation rule (`/api/ipam/vlan-translation-rules/`).

## Example Usage

```terraform
resource "netbox_vlan_translation_policy" "customer_a" {
  name = "customer-a"
}

resource "netbox_vlan_translation_rule" "customer_a_100" {
  policy_id   = netbox_vlan_translation_policy.customer_a.id
  local_vid   = 100
  remote_vid  = 2100
  description = "Customer VLAN 100 -> core 2100"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `local_vid` (Number) Numeric VLAN ID (1-4094).
- `policy_id` (Number) ID of the Vlan Translation Policy (`netbox_vlan_translation_policy`).
- `remote_vid` (Number) Numeric VLAN ID (1-4094).

### Optional

- `description` (String) Description. Defaults to an empty string.

### Read-Only

- `id` (Number) The numeric ID of the object in NetBox.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_vlan_translation_rule is imported by its NetBox object ID (the number in the object's URL).
terraform import netbox_vlan_translation_rule.example 123
```
