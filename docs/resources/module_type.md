---
page_title: "netbox_module_type Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox module type (/api/dcim/module-types/).
---

# netbox_module_type (Resource)

Manages a NetBox module type (`/api/dcim/module-types/`).

## Example Usage

```terraform
resource "netbox_manufacturer" "cisco" {
  name = "Cisco"
  slug = "cisco"
}
resource "netbox_module_type" "nm_8x" {
  manufacturer_id = netbox_manufacturer.cisco.id
  model           = "C9300-NM-8X"
  part_number     = "C9300-NM-8X"
  airflow         = "front-to-rear"
  weight          = 0.45
  weight_unit     = "kg"
  description     = "8x 10G SFP+ network module"
}
# Interface templates on the module type are replicated onto the device when
# a module of this type is installed; {module} becomes the bay position.
resource "netbox_interface_template" "uplinks" {
  count          = 8
  module_type_id = netbox_module_type.nm_8x.id
  name           = "TenGigabitEthernet1/{module}/${count.index + 1}"
  type           = "10gbase-x-sfpp"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `manufacturer_id` (Number) ID of the Manufacturer (`netbox_manufacturer`).
- `model` (String) Model.

### Optional

- `airflow` (String) Airflow. Valid values: `front-to-rear`, `rear-to-front`, `left-to-right`, `right-to-left`, `side-to-rear`, `passive`. Defaults to the NetBox server default when omitted.
- `attributes` (String) Attributes as a JSON document (`jsonencode({...})`).
- `comments` (String) Comments. Defaults to an empty string.
- `cooling_method` (String) Cooling Method. Valid values: `air`, `liquid`, `hybrid`, `immersion`. Defaults to the NetBox server default when omitted.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `end_of_life` (String) The date after which this module type is no longer supported by the manufacturer.
- `module_bay_type_ids` (Set of Number) IDs of the assigned Module Bay Type (`netbox_module_bay_type`). Defaults to an empty set.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `part_number` (String) Discrete part number (optional). Defaults to an empty string.
- `profile_id` (Number) ID of the Module Type Profile (`netbox_module_type_profile`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `weight` (Number) Weight. Defaults to the NetBox server default when omitted.
- `weight_unit` (String) Weight Unit. Valid values: `kg`, `g`, `lb`, `oz`. Defaults to the NetBox server default when omitted.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_module_type is imported by its NetBox object ID (the number in the object's URL).
terraform import netbox_module_type.example 123
```
