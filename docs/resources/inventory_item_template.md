---
page_title: "netbox_inventory_item_template Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox inventory item template (/api/dcim/inventory-item-templates/).
---

# netbox_inventory_item_template (Resource)

Manages a NetBox inventory item template (`/api/dcim/inventory-item-templates/`).

## Example Usage

```terraform
resource "netbox_manufacturer" "cisco" {
  name = "Cisco"
  slug = "cisco"
}
resource "netbox_device_type" "c9300" {
  manufacturer_id = netbox_manufacturer.cisco.id
  model           = "Catalyst 9300-48P"
  slug            = "c9300-48p"
  part_number     = "C9300-48P"
  u_height        = 1
}
resource "netbox_inventory_item_role" "psu" {
  name = "Power supply"
  slug = "power-supply"
}
resource "netbox_inventory_item_template" "psu" {
  count           = 2
  device_type_id  = netbox_device_type.c9300.id
  name            = "PSU ${count.index + 1}"
  label           = "PS${count.index + 1}"
  role_id         = netbox_inventory_item_role.psu.id
  manufacturer_id = netbox_manufacturer.cisco.id
  part_id         = "PWR-C1-715WAC"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `device_type_id` (Number) ID of the Device Type (`netbox_device_type`).
- `name` (String) {module} is accepted as a substitution for the module bay position when attached to a module type.

### Optional

- `component_id` (Number) Component Id. Defaults to the NetBox server default when omitted.
- `component_type` (String) Component Type.
- `description` (String) Description. Defaults to an empty string.
- `label` (String) Physical label. Defaults to an empty string.
- `manufacturer_id` (Number) ID of the Manufacturer (`netbox_manufacturer`).
- `parent_id` (Number) ID of the Inventory Item Template (`netbox_inventory_item_template`).
- `part_id` (String) Manufacturer-assigned part identifier. Defaults to an empty string.
- `role_id` (Number) ID of the Inventory Item Role (`netbox_inventory_item_role`).

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_inventory_item_template is imported by its NetBox object ID (the number in the object's URL).
terraform import netbox_inventory_item_template.example 123
```
