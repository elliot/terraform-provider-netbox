---
page_title: "netbox_region Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox region (/api/dcim/regions/).
---

# netbox_region (Resource)

Manages a NetBox region (`/api/dcim/regions/`).

## Example Usage

```terraform
resource "netbox_tag" "infra" {
  name = "infra"
  slug = "infra"
}

resource "netbox_region" "europe" {
  name        = "Europe"
  slug        = "europe"
  description = "EMEA operations"
}

# Regions nest: a child region refers to its parent by ID.
resource "netbox_region" "germany" {
  name        = "Germany"
  slug        = "de"
  parent_id   = netbox_region.europe.id
  description = "German data centres"
  tags        = [netbox_tag.infra.slug]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `slug` (String) Slug.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `parent_id` (Number) ID of the Region (`netbox_region`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_region is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_region.example 123
```
