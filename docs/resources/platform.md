---
page_title: "netbox_platform Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox platform (/api/dcim/platforms/).
---

# netbox_platform (Resource)

Manages a NetBox platform (`/api/dcim/platforms/`).

## Example Usage

```terraform
resource "netbox_manufacturer" "juniper" {
  name = "Juniper Networks"
  slug = "juniper"
}

resource "netbox_config_template" "junos_base" {
  name          = "Junos base"
  template_code = "system {\n  host-name {{ device.name }};\n}\n"
}

resource "netbox_platform" "junos" {
  name            = "Junos"
  slug            = "junos"
  manufacturer_id = netbox_manufacturer.juniper.id
  description     = "Juniper Junos OS"
}

# Platforms can nest (NetBox 4.4+), e.g. a specific release under the OS family.
resource "netbox_platform" "junos_evo" {
  name               = "Junos Evolved"
  slug               = "junos-evo"
  parent_id          = netbox_platform.junos.id
  manufacturer_id    = netbox_manufacturer.juniper.id
  config_template_id = netbox_config_template.junos_base.id
  description        = "Junos OS Evolved"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `slug` (String) Slug.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `config_template_id` (Number) ID of the Config Template (`netbox_config_template`).
- `custom_fields` (Dynamic) Custom field values as an object of field name to value, e.g. `{ cost_center = "CC-42", vlan_id = 5, owner_site = 12 }`. Selection fields take the choice value, object fields the related object ID, multi-value fields a list, JSON fields any value. Only keys present in the configuration are tracked; field names are validated against the NetBox definitions.
- `description` (String) Description. Defaults to an empty string.
- `manufacturer_id` (Number) ID of the Manufacturer (`netbox_manufacturer`).
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `parent_id` (Number) ID of the Platform (`netbox_platform`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_platform is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_platform.example 123
```
