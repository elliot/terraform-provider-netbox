---
page_title: "netbox_circuit_group Resource - NetBox"
subcategory: "Circuits"
description: |-
  Manages a NetBox circuit group (/api/circuits/circuit-groups/).
---

# netbox_circuit_group (Resource)

Manages a NetBox circuit group (`/api/circuits/circuit-groups/`).

## Example Usage

```terraform
resource "netbox_tenant" "example" {
  name = "Acme Corp"
  slug = "acme-corp"
}

resource "netbox_circuit_group" "example" {
  name        = "AMS uplinks"
  slug        = "ams-uplinks"
  tenant_id   = netbox_tenant.example.id
  description = "Redundant internet uplinks of the Amsterdam office"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `slug` (String) Slug.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_circuit_group can be imported by its NetBox object ID.
terraform import netbox_circuit_group.example 5
```
