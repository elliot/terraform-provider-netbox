---
page_title: "netbox_module_bay_type Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox module bay type (/api/dcim/module-bay-types/).
---

# netbox_module_bay_type (Resource)

Manages a NetBox module bay type (`/api/dcim/module-bay-types/`).

## Example Usage

```terraform
resource "netbox_manufacturer" "example" {
  name = "Example Networks"
  slug = "example-networks"
}

# Module bay types (NetBox 4.5+) classify module bays, e.g. by connector.
resource "netbox_module_bay_type" "sfp28" {
  name            = "SFP28 cage"
  slug            = "sfp28-cage"
  manufacturer_id = netbox_manufacturer.example.id
  color           = "9c27b0"
  description     = "Single SFP28 cage"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `slug` (String) Slug.

### Optional

- `color` (String) Color. Defaults to an empty string.
- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `manufacturer_id` (Number) ID of the Manufacturer (`netbox_manufacturer`).
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_module_bay_type is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_module_bay_type.example 123
```
