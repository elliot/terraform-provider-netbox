---
page_title: "netbox_location Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox location (/api/dcim/locations/).
---

# netbox_location (Resource)

Manages a NetBox location (`/api/dcim/locations/`).

## Example Usage

```terraform
resource "netbox_site" "fra1" {
  name = "FRA1"
  slug = "fra1"
}

resource "netbox_tenant" "acme" {
  name = "ACME Corp"
  slug = "acme"
}

resource "netbox_location" "floor_2" {
  name        = "Floor 2"
  slug        = "fra1-floor-2"
  site_id     = netbox_site.fra1.id
  status      = "active"
  description = "Second floor data hall"
}

# Locations nest within a site; the parent must belong to the same site.
resource "netbox_location" "cage_a" {
  name        = "Cage A"
  slug        = "fra1-floor-2-cage-a"
  site_id     = netbox_site.fra1.id
  parent_id   = netbox_location.floor_2.id
  tenant_id   = netbox_tenant.acme.id
  status      = "active"
  facility    = "Cage 2A"
  description = "ACME private cage"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `site_id` (Number) ID of the Site (`netbox_site`).
- `slug` (String) Slug.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `facility` (String) Local facility ID or description. Defaults to an empty string.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `parent_id` (Number) ID of the Location (`netbox_location`).
- `status` (String) Status. Valid values: `planned`, `staging`, `active`, `decommissioning`, `retired`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_location is imported by its NetBox object ID (the number in the object URL).
terraform import netbox_location.example 123
```
