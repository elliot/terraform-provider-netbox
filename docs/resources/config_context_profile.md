---
page_title: "netbox_config_context_profile Resource - NetBox"
subcategory: "Extras"
description: |-
  Manages a NetBox config context profile (/api/extras/config-context-profiles/).
---

# netbox_config_context_profile (Resource)

Manages a NetBox config context profile (`/api/extras/config-context-profiles/`).

## Example Usage

```terraform
# A profile validates the data of the config contexts assigned to it.
resource "netbox_config_context_profile" "ntp" {
  name        = "ntp"
  description = "Schema for NTP settings"
  schema = jsonencode({
    type = "object"
    properties = {
      ntp_servers = { type = "array", items = { type = "string" } }
    }
    required = ["ntp_servers"]
  })
}

resource "netbox_config_context" "ntp_emea" {
  name       = "ntp-emea"
  profile_id = netbox_config_context_profile.ntp.id
  data       = jsonencode({ ntp_servers = ["10.10.0.1", "10.10.0.2"] })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `data_source_id` (Number) ID of the Data Source (`netbox_data_source`).
- `description` (String) Description. Defaults to an empty string.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `schema` (String) A JSON schema specifying the structure of the context data for this profile.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `id` (Number) The numeric ID of the object in NetBox.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_config_context_profile can be imported by its NetBox object ID.
terraform import netbox_config_context_profile.example 123
```
