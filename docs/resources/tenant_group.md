---
page_title: "netbox_tenant_group Resource - NetBox"
subcategory: "Tenancy"
description: |-
  Manages a NetBox tenant group (/api/tenancy/tenant-groups/).
---

# netbox_tenant_group (Resource)

Manages a NetBox tenant group (`/api/tenancy/tenant-groups/`).

## Example Usage

```terraform
resource "netbox_tenant_group" "customers" {
  name        = "Customers"
  slug        = "customers"
  description = "External customers"
}

resource "netbox_tenant_group" "enterprise" {
  name      = "Enterprise customers"
  slug      = "enterprise-customers"
  parent_id = netbox_tenant_group.customers.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name.
- `slug` (String) Slug.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (String) Custom field values as a JSON object (`jsonencode({...})`). Only keys present in the configuration are tracked.
- `description` (String) Description. Defaults to an empty string.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `parent_id` (Number) ID of the Tenant Group (`netbox_tenant_group`).
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.

### Read-Only

- `created` (String) Created.
- `display` (String) Display.
- `id` (Number) The numeric ID of the object in NetBox.
- `last_updated` (String) Last Updated.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_tenant_group can be imported by its NetBox object ID.
terraform import netbox_tenant_group.example 4
```
