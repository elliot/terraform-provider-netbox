---
page_title: "netbox_permission Resource - NetBox"
subcategory: "Users"
description: |-
  Manages a NetBox permission (/api/users/permissions/).
---

# netbox_permission (Resource)

Manages a NetBox permission (`/api/users/permissions/`).

## Example Usage

```terraform
resource "netbox_user_group" "network_ops" {
  name = "network-ops"
}

resource "netbox_user" "automation" {
  username = "automation"
  password = var.automation_password
}

variable "automation_password" {
  type      = string
  sensitive = true
}

# object_types use the app_label.model form; actions are view, add, change,
# delete plus any custom action string. constraints is a Django queryset
# filter (an object or a list of objects) limiting the matching rows.
resource "netbox_permission" "network_ops_write" {
  name         = "network-ops: manage devices"
  description  = "Full control over devices and interfaces"
  object_types = ["dcim.device", "dcim.interface"]
  actions      = ["view", "add", "change", "delete"]
  group_ids    = [netbox_user_group.network_ops.id]
}

resource "netbox_permission" "automation_active_sites" {
  name         = "automation: view active sites"
  object_types = ["dcim.site"]
  actions      = ["view"]
  constraints  = jsonencode({ status = "active" })
  user_ids     = [netbox_user.automation.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `actions` (Set of String) The list of actions granted by this permission.
- `name` (String) Name.
- `object_types` (Set of String) Object Types.

### Optional

- `constraints` (String) Queryset filter matching the applicable objects of the selected type(s).
- `description` (String) Description. Defaults to an empty string.
- `enabled` (Boolean) Enabled. Defaults to the NetBox server default when omitted.
- `group_ids` (Set of Number) IDs of the assigned Group (`netbox_user_group`). Defaults to an empty set.
- `user_ids` (Set of Number) IDs of the assigned User (`netbox_user`). Defaults to an empty set.

### Read-Only

- `id` (Number) The numeric ID of the object in NetBox.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_permission is imported by its NetBox object ID (/api/users/permissions/).
terraform import netbox_permission.example 123
```
