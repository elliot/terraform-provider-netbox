---
page_title: "netbox_cable Resource - NetBox"
subcategory: "DCIM"
description: |-
  Manages a NetBox cable (/api/dcim/cables/).
---

# netbox_cable (Resource)

Manages a NetBox cable (`/api/dcim/cables/`).

## Example Usage

```terraform
resource "netbox_manufacturer" "cisco" {
  name = "Cisco"
  slug = "cisco"
}
resource "netbox_device_type" "c9300" {
  manufacturer_id = netbox_manufacturer.cisco.id
  model           = "Catalyst 9300-48P"
  slug            = "c9300-48p"
  part_number     = "C9300-48P"
  u_height        = 1
}
resource "netbox_site" "dc1" {
  name = "DC1"
  slug = "dc1"
}
resource "netbox_device_role" "access_switch" {
  name  = "Access switch"
  slug  = "access-switch"
  color = "2196f3"
}
resource "netbox_device" "sw01" {
  name           = "dc1-access-sw01"
  device_type_id = netbox_device_type.c9300.id
  role_id        = netbox_device_role.access_switch.id
  site_id        = netbox_site.dc1.id
  status         = "active"
}
resource "netbox_device" "sw02" {
  name           = "dc1-access-sw02"
  device_type_id = netbox_device_type.c9300.id
  role_id        = netbox_device_role.access_switch.id
  site_id        = netbox_site.dc1.id
}
resource "netbox_interface" "sw01_te1_1_1" {
  device_id = netbox_device.sw01.id
  name      = "TenGigabitEthernet1/1/1"
  type      = "10gbase-x-sfpp"
}
resource "netbox_interface" "sw02_te1_1_1" {
  device_id = netbox_device.sw02.id
  name      = "TenGigabitEthernet1/1/1"
  type      = "10gbase-x-sfpp"
}
resource "netbox_cable" "sw01_sw02" {
  a_terminations = [{ object_type = "dcim.interface", object_id = netbox_interface.sw01_te1_1_1.id }]
  b_terminations = [{ object_type = "dcim.interface", object_id = netbox_interface.sw02_te1_1_1.id }]
  type           = "smf-os2"
  status         = "connected"
  label          = "DC1-R01-0001"
  color          = "ffeb3b"
  length         = 3
  length_unit    = "m"
  description    = "Peer link"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `a_terminations` (Attributes List) A Terminations. (see [below for nested schema](#nestedatt--a_terminations))
- `b_terminations` (Attributes List) B Terminations. (see [below for nested schema](#nestedatt--b_terminations))
- `bundle_id` (Number) ID of the Cable Bundle (`netbox_cable_bundle`).
- `color` (String) Color. Defaults to an empty string.
- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (Dynamic) Custom field values as an object of field name to value, e.g. `{ cost_center = "CC-42", vlan_id = 5, owner_site = 12 }`. Selection fields take the choice value, object fields the related object ID, multi-value fields a list, JSON fields any value. Only keys present in the configuration are tracked; field names are validated against the NetBox definitions.
- `description` (String) Description. Defaults to an empty string.
- `label` (String) Label. Defaults to an empty string.
- `length` (Number) Length. Defaults to the NetBox server default when omitted.
- `length_unit` (String) Length Unit. Valid values: `km`, `m`, `cm`, `mi`, `ft`, `in`. Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `profile` (String) Profile. Valid values: `single-1c1p`, `single-1c2p`, `single-1c4p`, `single-1c6p`, `single-1c8p`, `single-1c12p`, `single-1c16p`, `trunk-2c1p`, `trunk-2c2p`, `trunk-2c4p`, `trunk-2c4p-shuffle`, `trunk-2c6p`, `trunk-2c8p`, `trunk-2c12p`, `trunk-4c1p`, `trunk-4c2p`, `trunk-4c4p`, `trunk-4c4p-shuffle`, `trunk-4c6p`, `trunk-4c8p`, `trunk-8c4p`, `breakout-1c2p-2c1p`, `breakout-1c4p-4c1p`, `breakout-1c6p-6c1p`, `breakout-1c8p-8c1p`, `breakout-2c4p-8c1p-shuffle`. Defaults to the NetBox server default when omitted.
- `status` (String) Status. Valid values: `connected`, `planned`, `decommissioning`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `type` (String) Type. Valid values: `cat3`, `cat5`, `cat5e`, `cat6`, `cat6a`, `cat7`, `cat7a`, `cat8`, `mrj21-trunk`, `dac-active`, `dac-passive`, `coaxial`, `rg-6`, `rg-8`, `rg-11`, `rg-59`, `rg-62`, `rg-213`, `lmr-100`, `lmr-200`, `lmr-400`, `mmf`, `mmf-om1`, `mmf-om2`, `mmf-om3`, `mmf-om4`, `mmf-om5`, `smf`, `smf-os1`, `smf-os2`, `aoc`, `power`, `usb`. Defaults to the NetBox server default when omitted.

### Read-Only

- `created` (String) Created.
- `id` (Number) The numeric ID of the object in NetBox.
- `url` (String) Url.

<a id="nestedatt--a_terminations"></a>
### Nested Schema for `a_terminations`

Required:

- `object_id` (Number) Object Id.
- `object_type` (String) Object Type.


<a id="nestedatt--b_terminations"></a>
### Nested Schema for `b_terminations`

Required:

- `object_id` (Number) Object Id.
- `object_type` (String) Object Type.

## Import

Import is supported using the following syntax:

```shell
# netbox_cable is imported by its NetBox object ID (the number in the object's URL).
terraform import netbox_cable.example 123
```
