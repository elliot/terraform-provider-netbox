---
page_title: "netbox_ip_range Resource - NetBox"
subcategory: "IPAM"
description: |-
  Manages a NetBox IP range (/api/ipam/ip-ranges/).
---

# netbox_ip_range (Resource)

Manages a NetBox IP range (`/api/ipam/ip-ranges/`).

## Example Usage

```terraform
resource "netbox_vrf" "acme" {
  name = "ACME-L3VPN"
}

resource "netbox_ipam_role" "dhcp" {
  name = "DHCP"
  slug = "dhcp"
}

# Both ends carry the prefix length of the enclosing network.
resource "netbox_ip_range" "dhcp_pool" {
  start_address = "10.10.20.100/24"
  end_address   = "10.10.20.199/24"
  vrf_id        = netbox_vrf.acme.id
  role_id       = netbox_ipam_role.dhcp.id
  status        = "active"
  mark_utilized = true
  description   = "DHCP pool for office VLAN 20"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `end_address` (String) End Address.
- `start_address` (String) Start Address.

### Optional

- `comments` (String) Comments. Defaults to an empty string.
- `custom_fields` (Dynamic) Custom field values as an object of field name to value, e.g. `{ cost_center = "CC-42", vlan_id = 5, owner_site = 12 }`. Selection fields take the choice value, object fields the related object ID, multi-value fields a list, JSON fields any value. Only keys present in the configuration are tracked; field names are validated against the NetBox definitions.
- `description` (String) Description. Defaults to an empty string.
- `mark_populated` (Boolean) Prevent the creation of IP addresses within this range. Defaults to the NetBox server default when omitted.
- `mark_utilized` (Boolean) Report space as fully utilized. Defaults to the NetBox server default when omitted.
- `owner_id` (Number) ID of the Owner (`netbox_owner`).
- `role_id` (Number) ID of the Ipam Role (`netbox_ipam_role`).
- `status` (String) Operational status of this range. Valid values: `active`, `reserved`, `deprecated`. Defaults to the NetBox server default when omitted.
- `tags` (Set of String) Slugs of the tags assigned to this object. Defaults to an empty set.
- `tenant_id` (Number) ID of the Tenant (`netbox_tenant`).
- `vrf_id` (Number) ID of the Vrf (`netbox_vrf`).

### Read-Only

- `created` (String) Created.
- `id` (Number) The numeric ID of the object in NetBox.
- `url` (String) Url.

## Import

Import is supported using the following syntax:

```shell
# netbox_ip_range is imported by its NetBox object ID (the number in the object's URL).
terraform import netbox_ip_range.example 123
```
